{
  "name": "healthpasstest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ healthpasstest",
    "create-local": "graph create --node http://localhost:8020/ healthpasstest",
    "remove-local": "graph remove --node http://localhost:8020/ healthpasstest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 healthpasstest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "ethereum-to-graphql": "^0.3.0"
  }
}
